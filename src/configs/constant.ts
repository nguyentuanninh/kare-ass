export enum UserStatus {
    INACTIVE = 'inactive',
    ACTIVE = 'active',
    REMOVED = 'removed',
}

export enum ActionType {
    VIEW = 'view',
    CREATE = 'create',
    EDIT = 'edit',
    DELETE = 'delete',
}

export enum ResourceType {
    USER = 'user',
    ROLE = 'role',
}
